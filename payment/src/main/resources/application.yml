spring:
  cloud:
    stream:
      kafka:
        binder:
          auto-add-partitions: true
      bindings:
        card-queue:
          destination: card-queue
          group: group2
        card-events:
          destination: card-events
          producer:
            partition-count: 2